@model WaffleOffer.Models.RegisterViewModel

@{
    ViewBag.Title = "Register";
}

<h4 class="dark-heading">Register</h4>

@Html.ValidationSummary(false)

@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <p>
        <button type="submit" class="btn btn-custom-submit">Register</button>
    </p>
}

